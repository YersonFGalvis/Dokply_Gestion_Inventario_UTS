export declare enum RoleType {
    USER = 1,
    ADMIN = 2
}
export declare enum ErrorsType {
    DTO = "ErrorDto",
    Duplicidad = "ErrorDuplicidad"
}
export declare function enumValidarClave(enumClass: any, clave: string): boolean;
